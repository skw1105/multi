<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.autocar.dao.CommentDao">
	<select id="count" resultType="int" parameterType="int">
		SELECT count(*) FROM comment_table where post_id = #{postId}
	</select>
	<insert id="insert" parameterType="Comment">
		insert into comment_table
		values
		(
		comment_table_SEQ.nextval, #{postId}, 0, #{content} ,
		'hotteok', sysdate, sysdate
		)
	</insert>

	<select id="getPage" resultType="Comment">
		select
		rownum, post_id, comment_no, parent_no, content, reg_date, writer
		from COMMENT_TABLE
		where post_id = #{postId} and rownum between #{start} and #{end}
		order by rownum desc
	</select>
</mapper>
